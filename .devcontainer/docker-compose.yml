version: '3.1'

services:

  db:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: openmrs

  openmrs:
    build: .
    image: beapen/openmrs-dev
    command: sleep infinity
    depends_on:
      - db

